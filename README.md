## 🎯 CRM для сбора и обработки заявок через iframe

Мини-CRM система, позволяющая собирать заявки через внешний виджет (iframe) и управлять ими в панели администратора.

## 🛠 Требования
* Docker & Docker Compose
* Node.js (для локальной разработки, если не через Sail)

## ⚙️ Установка и запуск

1. **Клонировать репозиторий:**
   ```bash
   git clone [https://github.com/aidar31/crm-tz.git](https://github.com/aidar31/crm-tz.git)
   cd crm-tz

```

2. **Настроить окружение:**
```bash
cp .env.example .env

```


3. **Установить PHP зависимости (через временный контейнер):**
```bash
docker run --rm \
    -u "$(id -u):$(id -g)" \
    -v "$(pwd):/var/www/html" \
    -w /var/www/html \
    laravelsail/php83-composer:latest \
    composer install --ignore-platform-reqs

```


4. **Запустить контейнеры:**
```bash
./vendor/bin/sail up -d

```


5. **Генерация ключа и настройка БД (миграции + сиды):**
```bash
./vendor/bin/sail artisan key:generate
./vendor/bin/sail artisan migrate --seed

```


6. **Установка JS зависимостей и сборка фронтенда:**
```bash
./vendor/bin/sail npm install
./vendor/bin/sail npm run dev

```



---

## 🔐 Авторизация и доступ

### Панель администратора

Доступна по адресу: http://localhost/admin/tickets

*Доступ разрешен только пользователям с ролью `manager`.*

**Тестовые данные для входа:**

* **Email:** `test@example.com`
* **Password:** `test12345678`

---

## 🔌 API Endpoints

* `POST /api/tickets` — Создание новой заявки (поддерживает загрузку файлов).
* `GET /api/tickets/statistics` — Получение статистики (день / неделя / месяц).

---

## 🧩 Виджет (Iframe)

Виджет доступен по адресу: `/widget`

Предназначен для встраивания на сторонние сайты. Отправка формы реализована через AJAX (Axios).

---

## 🏗 Архитектурные решения

* **Pattern Repository/Service:** Логика отделена от контроллеров и ORM.
* **Validation:** Все входящие данные фильтруются через `FormRequest`.
* **Media:** Работа с файлами через `Spatie MediaLibrary`.
* **ACL:** Права доступа и роли через `Spatie Permission`.
* **API:** Ответы стандартизированы с помощью `Eloquent Resources`.
